# 06_deploy

正式上传用 `upload_to_car.py`（先配 `upload_to_car.ini`，该 ini 已 gitignore）。

`_*.py` / `_*.sh` 和目录里的 jpg/png 是一次性探车脚本和抓图，**不要当启动入口**，也不要提交。反复用的流程在 `raicom` 包：`~/start_raicom_auto.sh` 和 `/raicom/*` 服务。
