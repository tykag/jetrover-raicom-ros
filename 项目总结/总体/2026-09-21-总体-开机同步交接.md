# 2026-09-21 开机同步交接

> 功能：充电后重新开机，把包传上车、停厂方自启、拉起 RAICOM 栈  
> 平台：ROS1 Melodic、JetRover 麦克纳姆、IP `10.217.187.242`  
> 上一份以谁为准：包结构、旧 `pick` 禁令和标定步骤看 `2026-09-21-总体-ROS包与下次开机交接.md`；开发习惯看 `2026-09-21-总体-开发流程.md`；语音词表看 `语音-WonderEchoPro/2026-09-21-语音WonderEchoPro-小鹿与执行运输任务.md`；**当前车上状态和下一步以本文为准**  
> 当前结论：包已上传并 `catkin build raicom --no-deps` 成功。厂方 `start_app_node.service` 已停。`~/start_raicom_auto.sh` 已拉起全向双地图栈，**语音未开**。旧 `pick` / `pick_aim` 仍作废。人必须先 2D Pose Estimate，再存新观察位和 aim，才能 `test_pick`。不要喊「执行运输任务」，不要 `/raicom/start`。

## 1. 这轮已经做完

| 项 | 结果 |
|----|------|
| 车 | 约 21:39 开机 11 分钟，IP 仍是 `10.217.187.242` |
| 上传 | `~/ros_ws/src/raicom/`（含 `raicom_voice.py`、`voice.launch`）、`~/start_raicom_auto.sh`、`~/start_raicom_voice.sh` |
| 编译 | `catkin build raicom --no-deps` → BUILD_OK |
| 厂方自启 | `inactive`（`sudo -n` 可用） |
| 启动 | `roslaunch raicom raicom.launch`，`SIDE=A`，`enable_voice=false`，`allow_high=false`，`grasp_mode=fixed` |
| 导航 | `/amcl` `/move_base` `/map_server` `/map_server_planning` `/joystick_control` `/rplidarNode` `/depth_cam` `/rviz` `/raicom_cam` |
| 规划 | `TebLocalPlannerROS/max_vel_y=0.15`，`amcl/odom_model_type=omni` |
| 任务 | `/raicom_auto` `/raicom_yolo`（TRT `best_fixed_320.engine`，`mode=mapping`） |
| 服务 | `/raicom/look_cargo` `/status` `/test_pick` `/start` `/abort` 已有；**没有** `/raicom/start_auto`（语音节点未起，符合预期） |

入口脚本改了一处：`sudo -n systemctl stop`，避免 SSH 无 TTY 时卡住。电脑：`tools/01_startup/start_raicom_auto.sh`。

## 2. 人现在要做（按顺序，不要跳）

栈已经在跑，**不要再执行** `~/start_raicom_auto.sh`。

1. 车旁看 RViz。用 **2D Pose Estimate**：点在车的真实位置，箭头沿通道指向待派送。以激光贴合黑围栏为准。
2. 手柄沿通道慢挪 20–40 cm 再回来。不抱车、不原地转。
3. 协方差大约位置 5 cm、朝向 2–3° 再存点；十几厘米或 7° 以上继续对，不存。
4. `rosservice call /raicom/look_cargo`，确认顶面贴纸仍可见、舵机到位。
5. 停在安全观察位后：`rostopic pub -1 /raicom/save_pose std_msgs/String "data: pick"`。
6. 把底盘人工对到**已知能抓住的低层位置**，YOLO 稳定后再 `raicom_arm.py save_aim`。只是画面里有方块，不要存 `pick_aim`。
7. 新 `pick` 和新 `pick_aim` 都有了，才允许一次 `rosservice call /raicom/test_pick`，再用 `/raicom/status` 看是动作完成还是夹到货。
8. 单抓成功后才测 `park*_approach → creep → place_low`。

## 3. 现在禁止

- 旧 `pick {x: 2.878, y: 0.685, yaw: -0.027}` 和旧 `pick_aim` / `pick_aim_low`。
- `/raicom/test_pick`、`/raicom/start`、口令「执行运输任务」，直到第 2 节第 6 步完成。
- `ENABLE_VOICE=1` 或 `roslaunch raicom voice.launch`（标定没完）。
- `allow_high:=true`、`grasp_mode:=3d`。
- 再叠一层厂方 `bringup.launch`。

新词「小鹿小鹿 / 执行运输任务」还要烧进 CI1302，否则盒子仍听旧唤醒词。烧固件与抓放标定互不替代。

## 4. 下一会话只读这些

1. 本文（车上现在怎样、人先做什么）
2. `2026-09-21-总体-ROS包与下次开机交接.md` 第 2–3 节（包布局和旧值）
3. 开发流程只在改代码时看

备份：`tools/01_startup/backup/2026-09-21-开机同步/`
