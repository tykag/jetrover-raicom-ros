# 2026-09-21 开机同步交接

> 功能：充电后重新开机，把包传上车、停厂方自启、拉起 RAICOM 栈  
> 平台：ROS1 Melodic、JetRover 麦克纳姆、IP `10.217.187.242`  
> 上一份以谁为准：包结构看 `2026-09-21-总体-ROS包与下次开机交接.md`；开发习惯看 `2026-09-21-总体-开发流程.md`；语音看 `语音-WonderEchoPro/2026-09-21-语音WonderEchoPro-小鹿与执行运输任务.md`；**当前车上状态和下一步以本文为准**  
> 当前结论：栈在跑，语音未开。2D Pose Estimate 和慢挪已做。**新观察位 `pick` 已写入车上 yaml**。YOLO 能稳定检出 gear，但方块偏左，**不是可抓位：禁止 `save_aim`、禁止 `test_pick`**。下一步：手柄对到已知能抓住的低层位置，再标 `pick_aim`。

> 更新日期：相对开机刚同步那版，已完成对墙、慢挪、存新 `pick`；旧 `pick_aim` 仍作废。

## 1. 栈

`~/start_raicom_auto.sh` 已起全向双地图 + YOLO + 状态机，`enable_voice=false`，`allow_high=false`，`grasp_mode=fixed`。厂方自启 `inactive`。不要再跑一遍启动脚本。YOLO 已切到 `detect`。

## 2. 新观察位

`look_cargo` 后待派送黄台可见。存 `pick` 时 AMCL 约 `x=2.919 y=0.742 yaw=-7.1°`，std ≈ `1.7cm / 4.1cm / 4.9°`。

```yaml
pick: {x: 2.91895, y: 0.74201, yaw: -0.12424}
```

车上：`~/yolo_models/raicom_waypoints.yaml`。电脑：`tools/04_vision_car/raicom_waypoints.yaml`。旧 `pick {x: 2.878, y: 0.685, yaw: -0.027}` 已覆盖。

当时检测约 `gear nx=0.274 ny=0.51 conf=0.89`。这只证明观察位能看见，不能当 aim。

## 3. 人现在要做

1. 手柄把底盘开到**已知能抓住的低层位置**（夹爪正对这块货）。
2. 确认 YOLO 仍稳定检出同一块 gear。
3. `python3 ~/ros_ws/src/raicom/scripts/raicom_arm.py save_aim`
4. 新 `pick_aim` 有了，才允许一次 `rosservice call /raicom/test_pick`，再用 `/raicom/status`。
5. 单抓成功后才测 `park*_approach → creep → place_low`。

## 4. 现在禁止

- 旧 `pick_aim {nx: 0.543, ny: 0.076}`、`pick_aim_low {nx: 0.517, ny: 0.207}`
- 在当前观察位 `save_aim`
- `/raicom/test_pick`、`/raicom/start`、口令「执行运输任务」，直到第 3 节第 3 步完成
- `ENABLE_VOICE=1`、`allow_high:=true`、`grasp_mode:=3d`

新词还要烧进 CI1302。

备份：`tools/01_startup/backup/2026-09-21-开机同步/`、`tools/04_vision_car/backup/2026-09-21-开机同步/`
