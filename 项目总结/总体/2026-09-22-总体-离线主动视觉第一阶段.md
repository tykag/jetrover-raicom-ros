# 2026-09-22 离线主动视觉第一阶段

> 功能：无车环境下落地 ROS 接口、纯算法库、主动观察/视觉对位和单个下层抓取编排  
> 平台：Windows 开发机 + ROS1 Melodic 包布局（未连 JetRover）  
> 上一份以谁为准：车上现场仍以 `2026-09-21-总体-开机同步交接.md` 为准；设计以 `docs/superpowers/specs/2026-09-22-raicom-active-vision-pick-deliver-design.md` 为准  
> 当前结论：第一阶段离线代码和 37 个单元测试已通过。现有 `nodes.launch` / `raicom.launch` 未切换。运动默认关闭。`catkin build raicom --no-deps` 未执行，代码未上车。

## 1. 备份与回滚

- 标签：`raicom-active-vision-pre-20260922` → `caef538`
- 备份分支：`backup/raicom-active-vision-pre-20260922`
- 文件快照：`tools/04_vision_car/backup/2026-09-22-active-vision-offline-baseline/`

包级回滚（不要 `git reset --hard`）：

```bash
git restore --source raicom-active-vision-pre-20260922 -- ROS1/ros_ws-main/ros_ws/src/raicom
```

YAML 快照需从 `raicom_yaml_baseline_caef538/` 手动拷回。

## 2. 已完成（仅离线）

- `raicom_core`：八槽位账本、跟踪、ROI 归属、深度过滤、有限搜索、视觉伺服、单块下层任务编排
- ROS 接口：`Detection2D`、`DetectionArray`、`ParkTarget`、`TaskState`、`AlignTarget`、`ArmTask`、`RunMission`、`SavePose`
- 薄节点：`raicom_alignment_node.py`、`raicom_arm_node.py`、`raicom_mission_node.py`
- canary：`launch/nodes_active_vision.launch` + `config/active_vision.yaml`（`enable_base_motion: false`、`enable_arm_motion: false`、`aim_calibrated: false`）
- 电脑验证：`python -m unittest discover` → 37 tests OK；`python -m compileall` 通过

## 3. 明确未完成

- 未连接小车，未部署
- 未跑 ROS Melodic `catkin build`
- 未示教 `look_high/mid/low`、`verify_*`，未标定新 aim/ROI
- 未开放园区黄色台面算法和八块连续运行
- 现有启动入口仍是 `~/start_raicom_auto.sh` / `roslaunch raicom raicom.launch`

## 4. 下次连车

1. 充电开机，按 2026-09-21 开机交接做 2D Pose Estimate
2. 在车上 `catkin build raicom --no-deps`
3. 不要改现有 `nodes.launch`；仅在单独终端试 `roslaunch raicom nodes_active_vision.launch`
4. 先示教观察/复查姿势和 `front_bottom_aim`，把 `aim_calibrated`/`roi_calibrated` 改为 true 后才允许运动
5. 运动开关保持 false，直到空载对位验收通过
