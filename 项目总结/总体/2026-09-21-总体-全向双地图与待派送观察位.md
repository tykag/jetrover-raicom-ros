> **本轮结束状态已更新**：下次开机以 `2026-09-21-总体-ROS包与下次开机交接.md` 为准。

# 2026-09-21 全向双地图与待派送观察位

> 功能：全向 + 双地图实车复测，以及自动流程卡在待派送认货  
> 平台：ROS1 Melodic、JetRover 麦克纳姆、车上桌面 RViz  
> 上一份以谁为准：低层抓放看 `2026-09-21-总体-低层抓放与导航交接.md`；为何拆图看 `2026-09-21-总体-台子雷达与双地图.md`；**当前下一步以本文为准**  
> 当前结论：全向导航和双地图已经能从智能分拣开到待派送停车点；自动抓放仍未跑通。旧 `pick` 太远，平视看不见台子。方块贴纸在顶面，认货要用 `look_cargo`。不要 `rosservice call /raicom/start`，直到存了新的 `pick`。

## 1. 车现在停在哪

人已经把车开回**智能分拣区**，并在 RViz 用 2D Pose Estimate 把箭头顺着通道指向待派送。关 RViz 时选了 **Discard**，厂方 `navigation_desktop.rviz` 没被覆盖。

抱过车之后必须重新对位，不能用抱之前的 AMCL。脚本注入的 `sort` 朝向约 33° 是旧示教值，车身摆正时会在图上显得斜。场地在地图里相对格子是斜的，要对的是**激光贴黑墙**，不是车模跟格子对齐。

SSH：`10.217.187.242`，`hiwonder` / `hiwonder`。导航应仍是 `drive_type:=omni`，`/map` 用 `explore_amcl`，costmap 用 `/map_planning`。

## 2. 本次已经验证

- 不必整场重建图。台子比赛不垫高，雷达扫不到。定位图去掉台子，规划图保留涂黑。
- `odom_model_type=omni`，TEB `max_vel_y=0.15`。已关掉 `rotate_recovery`。
- 从待派送空载开回 `sort`：AMCL 到点约 5 cm，`move_base` 成功。
- 看板两次锁定：`bolt→园区一`，`gear→园区二`。A 半场 `look_board`（右转看板）。
- 导航到旧 `pick` 在 AMCL 上算到点，**相机看不见待派送台子**。有一次画面里出现方块，是人把车抱近的，不是导航停对了。
- 贴纸在方块**顶面**。高视角 `look_cargo` 正对，能同时看到顶面和前面。不要再用 `look_front` 认货。
- 监视页不需要。人远程连小车桌面，终端开 RViz + `image_view`。

低层抓、园区一低层放仍以昨天示教为准，本次自动循环里没有成功抓到货。

## 3. 代码和路点

电脑：`tools/04_vision_car/`，车上：`~/yolo_models/`。

```text
raicom_auto.py     已上车：look_cargo + pick_aim；园区走 *_approach 再 creep，不直接 nav 到 park1
raicom_waypoints.yaml  已加 park1_approach / park2_approach；pick 仍是旧点，禁止当最终观察位
start_raicom_auto.sh   map:=explore_amcl planning_map:=explore_plan drive_type:=omni
                       并在 DISPLAY=:0 开 RViz 和相机；默认 _cycles:=2 _allow_high:=false
```

启动导航（已在跑就不要再杀 master）：

```bash
roslaunch hiwonder_navigation navigation.launch \
  map:=explore_amcl planning_map:=explore_plan \
  robot_name:=/ master_name:=/ drive_type:=omni
```

桌面看图：

```bash
source ~/ros_ws/devel/setup.zsh
export DISPLAY=:0
rviz -d ~/ros_ws/src/hiwonder_navigation/rviz/navigation_desktop.rviz
rosrun image_view image_view image:=/depth_cam/rgb/image_raw
```

关 RViz 选 Discard。急停：`rosservice call /raicom/abort` 或手柄。

备份：`tools/04_vision_car/backup/2026-09-21-全向双地图待派送/`  
地图：`maps_backup/2026-09-21-split-maps/`

## 4. 下次按这个做

1. 确认激光仍贴墙；漂移了再 2D Pose Estimate，箭头顺着通道朝待派送。
2. 手柄开到 `look_cargo` 能看清顶面贴纸、侧面也入画的位置（不要抱车）。
3. 对好墙后存观察位：

```bash
rostopic pub -1 /raicom/save_pose std_msgs/String "data: pick"
```

4. 只跑抓放一段：`rosservice call /raicom/test_pick`（会先 `goto pick`）。  
   或人已停在台前：`rosservice call /raicom/test_grasp`。
5. 抓住了再自动去 `park1_approach` / `park2_approach` 后 creep 放到作业位。
6. 不要改 `park1` / `park2` 作业位。高层 `place_down_high` 未实车标定，`allow_high` 保持 false，先各园区一块。
7. 8 块全自动要等高层放置示教之后。

未完成：新 `pick` 观察位、look_cargo 下 YOLO 认货、导航抓放闭环、高层放置、手眼 3D、完整 8 次循环。
